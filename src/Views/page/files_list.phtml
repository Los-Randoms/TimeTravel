<main class="container">
    <h1>Archivos<hr></h1>
    <table>
        <thead>
            <th>Id</th>
            <th>Nombre del archivo</th>
            <th>Tipo</th>
            <th>Tamaño</th>
            <th>Opciones</th>
        </thead>
        <tbody>
            <?php foreach($files as $file): ?>
            <tr>
                <td>
                    <?= $file->id ?>
                </td>
                <td>
                    <a href="<?= $file->url() ?>">
                        <?= strip_tags($file->filename) ?>
                    </a>
                </td>
                <td>
                    <?= strip_tags($file->mime) ?>
                </td>
                <td>
                    <?= $file->size ?> bytes
                </td>
                <td>
                
                <a role="button" href="/admin/archivos/eliminar?id=<?= $file->id ?>" type="submit">
                        <i class="mu mu-delete"></i>
                        <span>Eliminar</span>
                </td>
            </tr>
            <?php endforeach ?>
        </tbody>
    </table>
</main>
