<?php

use Modules\Utils;

?>
<main class="container">
    <div class="pfp">
        <?php if (!empty($pfp)) : ?>
            <img src="<?= $pfp->path ?>" class="contains" loading="lazy" alt="Imagen de perfil">
        <?php else : ?>
            <img src="/user.png" class="contains" loading="lazy" alt="Imagen de perfil">
        <?php endif ?>
    </div>
    <h1>
        <i class="mu mu-user"></i>
        <?= htmlspecialchars($user->username) ?>
        <!--       
                    <span class="banned">
                        <i class="mu mu-fail"></i>
                        Baneado
                    </span>
                -->
        <hr>
    </h1>
    <p class="user-info">
        <span class="key">
            <i class="mu mu-mail"></i>
            <b>Correo: </b>
        </span>
        <a href="mailto:<?= urlencode($user->email) ?>">
            <?= htmlspecialchars($user->email) ?>
        </a>
    </p>
    <p class="user-info">
        <span class="key">
            <i class="mu mu-lock"></i>
            <b>Rol: </b>
        </span>
        <span>
            <?= htmlspecialchars($user->email) ?>
        </span>
    </p>
    <p>
    <p class="user-info">
        <span class="key">
            <i class="mu mu-calendar"></i>
            <b>Se unió el: </b>
        </span>
        <span>
            <?= Utils::date($user->creation) ?>
        </span>
    </p>
    <?php if ($user->id === $_SESSION['user']->id) : ?>
        <a href="/perfil/editar" role="button"><i class="mu mu-edit"></i>Editar</a>
    <?php endif ?>
</main>
